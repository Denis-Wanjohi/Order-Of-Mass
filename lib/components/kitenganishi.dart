import 'package:flutter/material.dart';

class Kitenganishi extends StatelessWidget {
  const Kitenganishi({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("* * * *",style: TextStyle(fontSize: 20.0),));
  }
}